function generateHTML(data) {
  // insert code
  console.log(data.name);
  console.log(data.id);
  console.log(data.email);
  console.log(data.office);
  console.log(data.option);
  //   return `All this code`;
}

module.exports = generateHTML;
